// Homeコンポーネントの作成
import React from "react";
import Login from "../feature/auth/login";
// Homeコンポーネントの作成
const LoginPage = () => {
  return (
    <div>
      <Login />
    </div>
  );
};

export default LoginPage;
